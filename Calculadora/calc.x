#include "Estructuras.h"
program CALC_PROG{
	version CALC_VERS{
		float suma (struct Operacion)=1;
		float resta (struct Operacion)=2;
		float multiplicacion(struct Operacion)=3;
		float division(struct Operacion)=4;
	}=1;
} = 0x31111111;

